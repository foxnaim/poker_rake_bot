# 🎮 Android Эмулятор - Полная инструкция установки

## Шаг 1: Установка Android Studio

### 1.1. Установить через Homebrew

```bash
brew install --cask android-studio
```

Это займёт 5-10 минут (скачивается ~1GB).

### 1.2. Запустить Android Studio

```bash
open /Applications/Android\ Studio.app
```

При первом запуске появится мастер установки:

1. **Welcome** → Next
2. **Install Type** → выберите **Standard** → Next
3. **Verify Settings** → Finish
4. Подождите пока скачаются SDK компоненты (~2-3GB)

---

## Шаг 2: Создание виртуального устройства

### 2.1. Открыть Device Manager

После завершения установки:

1. На главном экране Android Studio нажмите **More Actions**
2. Выберите **Virtual Device Manager** (или через меню: Tools → Device Manager)

### 2.2. Создать новое устройство

1. Нажмите **Create Device** (большая кнопка +)

2. **Select Hardware:**
   - Категория: **Phone**
   - Выберите: **Pixel 6** (хорошее разрешение 1080x2400)
   - Нажмите **Next**

3. **System Image:**
   - Вкладка: **Recommended**
   - Выберите: **Tiramisu (API Level 33)** или **UpsideDownCake (API Level 34)**
   - Если рядом есть кнопка **Download** - нажмите и подождите
   - Нажмите **Next**

4. **Verify Configuration:**
   - Оставьте всё по умолчанию
   - ⚠️ **ВАЖНО:** Поставьте галочку **Enable Device Frame** (чтобы видеть рамку)
   - Нажмите **Finish**

---

## Шаг 3: Настройка эмулятора для ADB

### 3.1. Добавить ADB в PATH

ADB идёт вместе с Android Studio. Нужно добавить его в PATH:

```bash
# Для zsh (macOS по умолчанию)
echo 'export PATH=$PATH:$HOME/Library/Android/sdk/platform-tools' >> ~/.zshrc
source ~/.zshrc

# Для bash (если используете)
echo 'export PATH=$PATH:$HOME/Library/Android/sdk/platform-tools' >> ~/.bash_profile
source ~/.bash_profile
```

### 3.2. Проверить что ADB работает

```bash
adb --version
```

Должно показать:
```
Android Debug Bridge version 1.0.41
```

Если выдаёт ошибку - перезапустите терминал.

---

## Шаг 4: Запуск эмулятора

### 4.1. Запустить из Android Studio

В Device Manager:
1. Найдите ваше устройство (Pixel 6)
2. Нажмите кнопку ▶️ (Play) справа
3. Подождите 30-60 секунд пока загрузится Android

Вы увидите окно с Android телефоном.

### 4.2. Проверить подключение через ADB

Откройте новый терминал:

```bash
adb devices
```

Должно показать:
```
List of devices attached
emulator-5554    device
```

✅ Отлично! Эмулятор подключен.

---

## Шаг 5: Настройка эмулятора

### 5.1. Включить режим разработчика

В эмуляторе (на экране Android):

1. Откройте **Settings** (иконка шестерёнки)
2. Прокрутите вниз → **About emulated device**
3. Найдите **Build number**
4. **Тапните 7 раз** по Build number
5. Появится сообщение "You are now a developer!"

### 5.2. Включить USB debugging

1. Вернитесь в **Settings**
2. Найдите **System** → **Developer options**
3. Включите **USB debugging** (переключатель вправо)
4. Также включите **Stay awake** (экран не будет гаснуть)

### 5.3. Настроить экран

В окне эмулятора справа есть панель с кнопками:

1. Нажмите на три точки **⋮** (Extended controls)
2. Выберите **Settings**
3. **Emulated Performance:**
   - Graphics: **Hardware - GLES 2.0**
4. **Window:**
   - Размер окна должен быть комфортным (можно изменить кнопками +/-)

---

## Шаг 6: Установка PPPoker

### Вариант A: Через Google Play Store (если есть аккаунт)

В эмуляторе:
1. Откройте **Play Store**
2. Войдите в Google аккаунт
3. Найдите "PPPoker"
4. Установите

### Вариант B: Через APK файл (рекомендуется)

**6.1. Скачать APK PPPoker**

Откройте браузер на Mac и скачайте PPPoker APK с одного из источников:
- https://pppoker.net (официальный сайт)
- https://apkpure.com/pppoker (зеркало)

Или через терминал:
```bash
cd ~/Downloads
# Замените URL на актуальный
curl -L -o pppoker.apk "https://pppoker.net/download/pppoker.apk"
```

**6.2. Установить APK на эмулятор**

```bash
cd ~/Downloads
adb install pppoker.apk
```

Вы увидите:
```
Performing Streamed Install
Success
```

**6.3. Запустить PPPoker**

В эмуляторе:
1. Откройте App Drawer (свайп вверх от нижнего края)
2. Найдите иконку PPPoker
3. Запустите приложение

---

## Шаг 7: Настройка PPPoker

### 7.1. Создать/Войти в аккаунт

1. При первом запуске создайте аккаунт или войдите
2. Следуйте инструкциям в приложении

### 7.2. Найти стол и сесть играть

1. В PPPoker найдите клуб или публичный стол
2. Выберите стол **NL10** или **NL50** (6-max)
3. Сядьте за стол с полным стеком
4. Дождитесь начала раздачи

### 7.3. Важные настройки экрана

**В эмуляторе:**
- Убедитесь что экран в **вертикальном** режиме (portrait)
- Если повернут горизонтально - кликните на кнопку rotate слева на панели эмулятора

**В PPPoker:**
- Настройки → Display → Brightness: максимум
- Отключите анимации если есть такая опция

---

## Шаг 8: Получить разрешение экрана

Это важно для правильной работы бота:

```bash
adb shell wm size
```

Вы должны увидеть:
```
Physical size: 1080x2400
```

Запомните эти числа - они понадобятся для настройки бота.

---

## Шаг 9: Тест скриншота

Проверим что бот сможет делать скриншоты:

```bash
cd /Users/who/Downloads/prod/poker_rake_bot
adb exec-out screencap -p > test_screenshot.png
open test_screenshot.png
```

Вы должны увидеть скриншот экрана эмулятора с PPPoker.

✅ Если видите - всё работает!

---

## Шаг 10: Первый запуск бота (DRY RUN)

### 10.1. Проверить зависимости Python

```bash
cd /Users/who/Downloads/prod/poker_rake_bot

# Проверить что всё установлено
python3 -c "import cv2, pytesseract, PIL, numpy, requests; print('✅ Все зависимости OK')"
```

Если ошибка - установите:
```bash
pip3 install opencv-python pillow pytesseract numpy requests
```

### 10.2. Убедиться что Backend запущен

В другом терминале:
```bash
cd /Users/who/Downloads/prod/poker_rake_bot
docker-compose up -d

# Проверить что работает
curl http://localhost:8000/health
```

### 10.3. Запустить бота в тестовом режиме

```bash
python3 pppoker_adb_bot_full.py
```

Вы увидите:
```
🤖 Инициализация PPPoker Bot...
✅ Подключено к устройству: emulator-5554
📱 Размер экрана: 1080x2400
🎯 Parser инициализирован для 1080x2400
✅ Бот готов к работе!

⚠️  РЕЖИМ DRY_RUN АКТИВЕН
⚠️  Бот НЕ будет кликать по кнопкам
⚠️  Для реальной игры измените DRY_RUN = False

🎮 Запуск основного цикла...
```

Подождите пока начнётся раздача в PPPoker. Бот покажет что распознал:

```
🎴 Карты героя: AsKh
🎴 Борд: ---
💰 Пот: $1.50
🎯 Улица: preflop
🎲 Дилер: позиция 3
📍 Позиция героя: 2 от дилера
👥 Активных игроков: 6
💵 Последний рейз: $0.50
💵 Стек героя: $10.00

🎯 Наш ход!
🤖 Решение AI: RAISE
   Сумма: $1.50
🔷 [DRY RUN] Клик по (650, 920)
```

**🔷 [DRY RUN]** означает что бот показывает что будет делать, но **НЕ кликает**.

---

## Шаг 11: Проверка debug изображений

После того как бот поработал 1-2 минуты, остановите его (Ctrl+C) и проверьте:

```bash
ls -la debug_*.png
```

Вы должны увидеть файлы:
- `debug_hero_cards.png` - ваши карты
- `debug_board.png` - карты борда
- `debug_pot.png` - размер пота
- `debug_stack_0.png` ... `debug_stack_5.png` - стеки игроков
- `debug_bet_0.png` ... `debug_bet_5.png` - ставки
- `debug_players.png` - все игроки с отметками

Откройте главный файл:
```bash
open debug_players.png
```

**Что проверить:**
- ✅ Все игроки помечены кругами? (зелёный = дилер, жёлтый = остальные)
- ✅ Круги находятся над аватарами игроков?
- ✅ Видно карты в `debug_hero_cards.png`?
- ✅ Видно цифры стека в `debug_stack_0.png`?

### Если координаты неправильные

Если круги игроков не на месте, нужно подстроить координаты:

```bash
nano pppoker_adb_bot_full.py
```

Найдите (строка ~47):
```python
PLAYER_SCREEN_POSITIONS = {
    0: (0.50, 0.75),  # Hero (внизу по центру)
    1: (0.15, 0.65),  # Лево-низ
    2: (0.10, 0.35),  # Лево-верх
    3: (0.50, 0.20),  # Верх по центру
    4: (0.90, 0.35),  # Право-верх
    5: (0.85, 0.65),  # Право-низ
}
```

Это проценты от размера экрана. Например:
- `(0.50, 0.75)` = 50% ширины, 75% высоты
- Измените числа чтобы круги попали на аватары игроков

---

## Шаг 12: Включить реальную игру

Когда убедитесь что всё распознаётся правильно:

```bash
nano pppoker_adb_bot_full.py
```

Найдите строку (~45):
```python
DRY_RUN = True
```

Измените на:
```python
DRY_RUN = False
```

Также отключите debug режим для скорости:
```python
DEBUG_MODE = False
SAVE_DEBUG_IMAGES = False
```

Сохраните: **Ctrl+O** → **Enter** → **Ctrl+X**

Запустите снова:
```bash
python3 pppoker_adb_bot_full.py
```

Теперь бот будет **РЕАЛЬНО КЛИКАТЬ** по кнопкам в эмуляторе!

---

## 🎯 Полезные команды для эмулятора

### Перезапустить эмулятор
```bash
# Остановить
adb emu kill

# Запустить снова из Android Studio
# Device Manager → ▶️
```

### Очистить данные PPPoker
```bash
adb shell pm clear net.pppoker.pppoker
```

### Сделать скриншот вручную
```bash
adb exec-out screencap -p > screenshot.png
open screenshot.png
```

### Посмотреть логи Android
```bash
adb logcat | grep PPPoker
```

### Ввести текст в эмулятор
```bash
adb shell input text "Hello"
```

### Клик по координатам
```bash
adb shell input tap 500 1000
```

---

## ⚠️ Troubleshooting

### Проблема: "adb: command not found"

**Решение:**
```bash
# Найти где установлен ADB
find ~/Library/Android/sdk -name adb 2>/dev/null

# Добавить в PATH
export PATH=$PATH:~/Library/Android/sdk/platform-tools
adb --version
```

### Проблема: "no devices found"

**Решение:**
```bash
# Перезапустить ADB
adb kill-server
adb start-server
adb devices

# Или перезапустить эмулятор
# Device Manager → Stop → Start
```

### Проблема: Эмулятор тормозит

**Решение:**
1. Extended controls (⋮) → Settings → Emulated Performance
2. Graphics: Hardware - GLES 2.0
3. Boot option: Cold boot
4. RAM: увеличить до 4096 MB
5. Закрыть другие приложения на Mac

### Проблема: PPPoker не устанавливается

**Решение:**
```bash
# Проверить что APK скачан
ls -lh ~/Downloads/pppoker.apk

# Переустановить
adb uninstall net.pppoker.pppoker
adb install ~/Downloads/pppoker.apk

# Если не знаете package name
adb shell pm list packages | grep poker
```

### Проблема: Бот не распознаёт карты

**Причины:**
1. Низкое разрешение эмулятора
2. PPPoker в тёмной теме
3. Неправильные координаты

**Решение:**
1. Проверить разрешение: `adb shell wm size` (должно быть 1080x2400 или больше)
2. Увеличить яркость в PPPoker
3. Посмотреть `debug_hero_cards.png` - что там видно?
4. Настроить координаты в коде

### Проблема: Backend API не отвечает

**Решение:**
```bash
# Проверить Docker
docker ps

# Перезапустить
docker-compose down
docker-compose up -d

# Проверить логи
docker-compose logs api

# Тест API
curl -H "X-API-Key: dev_admin_key" http://localhost:8000/api/v1/decision
```

---

## 📊 Мониторинг бота

### Консоль бота
Бот выводит подробную информацию:
```
🎴 Карты героя: AsKh
🎴 Борд: 9d7c2s
💰 Пот: $3.50
👥 Активных игроков: 4
🤖 Решение AI: RAISE $2.50
✅ Клик: (650, 920)
📊 Сыграно рук: 42
```

### Web Dashboard
Откройте браузер: http://localhost:3000

Вы увидите:
- Real-time статистику
- Винрейт
- Количество рук
- Графики

### Debug режим
При `DEBUG_MODE = True` бот выводит подробную информацию о каждом игроке:
```
👤 Позиция 0: Stack=100.00, Bet=0.00, Dealer=False
👤 Позиция 1: Stack=95.50, Bet=0.50, Dealer=False
👤 Позиция 3: Stack=110.00, Bet=0.00, Dealer=True
```

---

## 🎮 Готово!

Теперь у вас есть:
- ✅ Android эмулятор
- ✅ PPPoker установлен
- ✅ ADB подключен
- ✅ Бот запущен и работает

### Следующие шаги:

1. **Дайте боту поиграть 10-20 рук в DRY_RUN режиме**
2. **Проверьте все debug_*.png файлы**
3. **Убедитесь что распознавание точное**
4. **Включите DRY_RUN = False**
5. **Следите за результатами в Dashboard**

**🎰 Удачи за столами! ♠️♥️♦️♣️**
