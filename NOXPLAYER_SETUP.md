# 🎮 NoxPlayer - Быстрая установка и настройка

## Шаг 1: Установка NoxPlayer

После того как скачал **nox_setup_v7.x.x.dmg**:

1. Открой DMG файл (двойной клик)
2. Перетащи **NoxPlayer** в папку **Applications**
3. Дождись копирования
4. Открой **Applications** → **NoxPlayer**

При первом запуске macOS может показать предупреждение:
- "NoxPlayer can't be opened because it is from an unidentified developer"
- Решение: **System Settings** → **Privacy & Security** → **Open Anyway**

---

## Шаг 2: Первый запуск NoxPlayer

1. Запустится **NoxPlayer**
2. Начнётся инициализация (1-2 минуты)
3. Появится окно с Android

**Важно:** Оставь настройки по умолчанию при первом запуске.

---

## Шаг 3: Настройка ADB для NoxPlayer

NoxPlayer имеет **встроенный ADB**, но нам нужно подключить наш системный ADB к нему.

### 3.1. Найти ADB порт NoxPlayer

В NoxPlayer:
1. Нажми на **Settings** (шестерёнка в правом верхнем углу)
2. Найди раздел **Connection**
3. Запомни **ADB Port** (обычно **62001** или **62025**)

### 3.2. Подключить ADB

```bash
# Узнать IP (обычно localhost)
adb connect 127.0.0.1:62001

# Проверить подключение
adb devices
```

Должно показать:
```
List of devices attached
127.0.0.1:62001    device
```

✅ Готово! ADB подключен к NoxPlayer!

---

## Шаг 4: Настройка Android в NoxPlayer

В окне NoxPlayer (на экране Android):

### 4.1. Включить режим разработчика
1. **Settings** (иконка шестерёнки)
2. Прокрутить вниз → **About tablet**
3. Найти **Build number**
4. **Тапнуть 7 раз** по Build number
5. Появится: "You are now a developer!"

### 4.2. Включить USB Debugging
1. Вернуться в **Settings**
2. **Developer options** (новый пункт появился)
3. Включить **USB debugging** ✅
4. Включить **Stay awake** ✅

---

## Шаг 5: Настроить разрешение экрана

Для правильной работы бота нужно правильное разрешение.

### В NoxPlayer (справа на панели):
1. Нажать на **Settings** (шестерёнка)
2. **Advanced**
3. **Resolution**: выбрать **1080x1920** (Tablet)
4. **Apply** → **Restart**

После перезапуска проверь:
```bash
adb connect 127.0.0.1:62001
adb shell wm size
```

Должно показать:
```
Physical size: 1080x1920
```

---

## Шаг 6: Установить PPPoker APK

После того как скачал **pppoker.apk**:

### Вариант A: Через интерфейс NoxPlayer
1. Перетащи **pppoker.apk** в окно NoxPlayer
2. Нажми **Install**
3. Дождись установки

### Вариант B: Через ADB
```bash
cd ~/Downloads
adb -s 127.0.0.1:62001 install pppoker.apk
```

Увидишь:
```
Performing Streamed Install
Success
```

---

## Шаг 7: Запустить PPPoker

В NoxPlayer:
1. Открой **App Drawer** (иконка приложений внизу)
2. Найди **PPPoker**
3. Запусти
4. Создай аккаунт или войди
5. Найди клуб/стол **NL10** или **NL50** (6-max)
6. Сядь за стол с полным стеком

---

## Шаг 8: Проверить размер экрана для бота

```bash
adb -s 127.0.0.1:62001 shell wm size
```

Запомни эти числа - они нужны для бота.

---

## Шаг 9: Тест скриншота

Проверим что бот сможет делать скриншоты:

```bash
cd /Users/who/Downloads/prod/poker_rake_bot
adb -s 127.0.0.1:62001 exec-out screencap -p > test_screenshot.png
open test_screenshot.png
```

Должен увидеть скриншот PPPoker ✅

---

## Шаг 10: Обновить бота для NoxPlayer

Нужно указать боту правильный device ID:

```bash
nano pppoker_adb_bot_full.py
```

В начале файла (~строка 103) найди метод `_connect`:

```python
def _connect(self):
    """Подключается к устройству"""
    try:
        result = subprocess.run(['adb', 'devices'], capture_output=True, text=True)
        lines = result.stdout.strip().split('\n')[1:]

        devices = [line.split()[0] for line in lines if line.strip() and 'device' in line]

        if not devices:
            raise Exception("Не найдено подключенных Android устройств. Выполните: adb devices")

        self.device_id = devices[0]  # ← Это автоматически выберет первое устройство
```

Это должно работать автоматически, но если нужно явно указать NoxPlayer:

```python
# Добавь перед проверкой devices:
# Попробовать подключиться к NoxPlayer
subprocess.run(['adb', 'connect', '127.0.0.1:62001'], capture_output=True)
```

Сохрани: **Ctrl+O** → **Enter** → **Ctrl+X**

---

## Шаг 11: Запустить бота (DRY RUN)

```bash
cd /Users/who/Downloads/prod/poker_rake_bot

# Сначала подключимся к NoxPlayer
adb connect 127.0.0.1:62001

# Запустить бота
python3 pppoker_adb_bot_full.py
```

Бот должен показать:
```
✅ Подключено к устройству: 127.0.0.1:62001
📱 Размер экрана: 1080x1920
🎯 Parser инициализирован

🎴 Карты героя: AsKh
🎴 Борд: ---
💰 Пот: $1.50
🔷 [DRY RUN] Клик по (650, 920)
```

---

## Шаг 12: Проверить debug файлы

```bash
ls -la debug_*.png
open debug_players.png
```

Проверь:
- ✅ Игроки помечены кругами (зелёный = дилер)
- ✅ Круги над аватарами игроков
- ✅ Карты видны в `debug_hero_cards.png`
- ✅ Стеки видны в `debug_stack_0.png`

---

## Шаг 13: Включить реальную игру

Когда всё работает правильно:

```bash
nano pppoker_adb_bot_full.py
```

Изменить:
```python
DRY_RUN = False  # Было: True
DEBUG_MODE = False
SAVE_DEBUG_IMAGES = False
```

Сохранить и запустить:
```bash
python3 pppoker_adb_bot_full.py
```

**Бот начнёт РЕАЛЬНО играть!** 🎰

---

## 🔧 Troubleshooting

### Проблема: "adb: device offline"

```bash
adb disconnect 127.0.0.1:62001
adb connect 127.0.0.1:62001
adb devices
```

### Проблема: "adb: no devices found"

1. Проверь что NoxPlayer запущен
2. В NoxPlayer Settings → Connection убедись что ADB включен
3. Узнай правильный порт (обычно 62001 или 62025)
4. Подключись заново:
```bash
adb connect 127.0.0.1:62001
```

### Проблема: NoxPlayer тормозит

В настройках NoxPlayer:
- **Performance**:
  - CPU: 4 cores
  - RAM: 4096 MB
  - Graphics rendering mode: DirectX

### Проблема: PPPoker не устанавливается

```bash
# Проверить что ADB видит устройство
adb devices

# Переустановить
adb -s 127.0.0.1:62001 uninstall com.pppoker.main
adb -s 127.0.0.1:62001 install pppoker.apk
```

---

## ⚡ Полезные команды

```bash
# Подключиться к NoxPlayer
adb connect 127.0.0.1:62001

# Проверить устройства
adb devices

# Скриншот
adb -s 127.0.0.1:62001 exec-out screencap -p > screenshot.png

# Клик
adb -s 127.0.0.1:62001 shell input tap 500 1000

# Текст
adb -s 127.0.0.1:62001 shell input text "Hello"

# Перезапустить NoxPlayer
# Просто закрой и открой заново NoxPlayer
```

---

## 🎯 Готово!

Теперь у тебя:
- ✅ NoxPlayer работает
- ✅ ADB подключен
- ✅ PPPoker установлен
- ✅ Бот играет автоматически
- ✅ Dashboard показывает статистику

**Удачи за столами! 🎰♠️♥️♦️♣️**
