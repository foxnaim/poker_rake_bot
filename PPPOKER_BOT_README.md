# 🎰 PPPoker ADB Bot - Полное руководство

## 📋 Содержание

1. [Что это?](#что-это)
2. [Возможности](#возможности)
3. [Установка](#установка)
4. [Настройка Android устройства](#настройка-android-устройства)
5. [Запуск бота](#запуск-бота)
6. [Как это работает](#как-это-работает)
7. [Режимы работы](#режимы-работы)
8. [Troubleshooting](#troubleshooting)

---

## Что это?

**PPPoker ADB Bot** - это полностью автоматический покерный бот, который подключается к приложению PPPoker на Android устройстве через **Android Debug Bridge (ADB)** и играет в покер автоматически.

### Принцип работы

```
Android устройство  →  ADB  →  Скриншоты  →  OCR/CV  →  Backend AI  →  Действия
     (PPPoker)              (screenshot)    (распознавание)  (решение)   (клики)
```

---

## Возможности

### ✅ Полностью реализовано

- **Автоматическое распознавание экрана:**
  - ✅ Карты героя (ваши карты)
  - ✅ Карты борда (flop, turn, river)
  - ✅ Размер пота
  - ✅ Стеки всех игроков (6-max)
  - ✅ Ставки всех игроков
  - ✅ Позиция баттона дилера (через Computer Vision)
  - ✅ Определение улицы (preflop, flop, turn, river)
  - ✅ Относительная позиция от дилера

- **Интеграция с Backend:**
  - ✅ Отправка game state в AI для принятия решения
  - ✅ Получение решения (fold, call, raise)
  - ✅ Логирование всех раздач

- **Автоматическое выполнение действий:**
  - ✅ Клик по кнопке Fold
  - ✅ Клик по кнопке Call/Check
  - ✅ Клик по кнопке Raise (с вводом суммы)

- **Безопасность:**
  - ✅ Режим DRY_RUN (показывает что будет делать, но не кликает)
  - ✅ Debug режим с сохранением скриншотов
  - ✅ Обработка ошибок

---

## Установка

### 1. Установка Python зависимостей

```bash
pip install opencv-python pillow pytesseract numpy requests
```

### 2. Установка Tesseract OCR

**macOS:**
```bash
brew install tesseract
```

**Ubuntu/Debian:**
```bash
sudo apt-get install tesseract-ocr
```

**Windows:**
Скачайте установщик с https://github.com/UB-Mannheim/tesseract/wiki

### 3. Установка ADB (Android Debug Bridge)

**macOS:**
```bash
brew install android-platform-tools
```

**Ubuntu/Debian:**
```bash
sudo apt-get install adb
```

**Windows:**
Скачайте SDK Platform Tools: https://developer.android.com/studio/releases/platform-tools

### 4. Проверка установки

```bash
# Проверка ADB
adb version

# Проверка Tesseract
tesseract --version

# Проверка Python пакетов
python3 -c "import cv2, pytesseract, PIL; print('OK')"
```

---

## Настройка Android устройства

### Шаг 1: Включить режим разработчика

1. Откройте **Настройки** → **О телефоне**
2. Найдите **Номер сборки** (Build number)
3. Нажмите на него **7 раз**
4. Появится сообщение "Вы стали разработчиком!"

### Шаг 2: Включить USB отладку

1. Откройте **Настройки** → **Для разработчиков**
2. Включите **USB отладку** (USB debugging)
3. Включите **Оставаться в режиме ожидания** (Stay awake) - чтобы экран не гас

### Шаг 3: Подключить устройство

**Вариант A: USB кабель (рекомендуется)**
1. Подключите Android к компьютеру через USB
2. На телефоне появится запрос "Разрешить USB отладку?" - нажмите **ОК**
3. Проверьте подключение:
   ```bash
   adb devices
   ```
   Должно появиться:
   ```
   List of devices attached
   XXXXXXXXXXXXX   device
   ```

**Вариант B: WiFi (для продвинутых)**
1. Подключите устройство по USB
2. Выполните:
   ```bash
   adb tcpip 5555
   adb connect <IP_АДРЕС_ТЕЛЕФОНА>:5555
   ```
3. Теперь можно отключить USB

### Шаг 4: Настроить PPPoker

1. Откройте приложение PPPoker
2. Войдите в аккаунт
3. Зайдите за стол (NL10 или NL50)
4. Убедитесь что игра в **портретном режиме** (вертикально)

---

## Запуск бота

### Режим 1: DRY RUN (безопасное тестирование)

По умолчанию бот запускается в режиме **DRY_RUN = True**, который только **показывает** что будет делать, но **НЕ кликает** по кнопкам.

```bash
python3 pppoker_adb_bot_full.py
```

Вы увидите:
```
⚠️  РЕЖИМ DRY_RUN АКТИВЕН
⚠️  Бот НЕ будет кликать по кнопкам
⚠️  Для реальной игры измените DRY_RUN = False

🎴 Карты героя: AsKh
🎴 Борд: 9d7c2s
💰 Пот: $3.50
🎯 Улица: flop
🎲 Дилер: позиция 3
📍 Позиция героя: 0 от дилера
👥 Активных игроков: 4

🎯 Наш ход!
🤖 Решение AI: RAISE
   Сумма: $2.50
🔷 [DRY RUN] Клик по (650, 920)
```

**Проверьте debug изображения:**
```bash
ls -la debug_*.png
```

Вы увидите файлы:
- `debug_hero_cards.png` - ваши карты
- `debug_board.png` - борд
- `debug_pot.png` - пот
- `debug_stack_0.png` - стек позиции 0
- `debug_bet_0.png` - ставка позиции 0
- `debug_players.png` - все игроки с отметками

**Проверьте что всё распознаётся правильно!**

### Режим 2: РЕАЛЬНАЯ ИГРА

Когда убедитесь что всё работает правильно:

1. Откройте файл `pppoker_adb_bot_full.py`
2. Найдите строку:
   ```python
   DRY_RUN = True
   ```
3. Измените на:
   ```python
   DRY_RUN = False
   ```
4. Также отключите debug режим:
   ```python
   DEBUG_MODE = False
   SAVE_DEBUG_IMAGES = False
   ```
5. Запустите:
   ```bash
   python3 pppoker_adb_bot_full.py
   ```

**⚠️ ВНИМАНИЕ: Теперь бот будет РЕАЛЬНО кликать по кнопкам!**

---

## Как это работает

### 1. Цикл работы бота

```python
while True:
    # 1. Делаем скриншот Android экрана
    screenshot = adb.screenshot()

    # 2. Распознаём состояние игры (OCR + Computer Vision)
    game_state = parser.parse_full_game_state(screenshot)

    # 3. Проверяем - наш ли ход?
    if not is_hero_turn(screenshot):
        continue

    # 4. Отправляем game_state в Backend AI
    decision = api.get_decision(game_state)

    # 5. Выполняем действие (fold, call, raise)
    execute_action(decision)

    # 6. Логируем раздачу
    api.log_hand(game_state, decision, result)
```

### 2. Технологии распознавания

**OCR (Optical Character Recognition):**
- Используется **Tesseract** для распознавания текста
- Карты: `AsKh` (ранг + масть)
- Числа: стеки, ставки, пот

**Computer Vision:**
- Используется **OpenCV** для поиска баттона дилера
- Поиск белого круглого объекта с буквой D
- HSV цветовое пространство для фильтрации
- Анализ контуров на "круглость"

### 3. Препроцессинг изображений

Для улучшения точности OCR:
1. **Grayscale** - перевод в чёрно-белое
2. **Histogram Equalization** - выравнивание гистограммы (контраст)
3. **Binarization** - порог (OTSU)
4. **Upscaling** - увеличение в 2-3 раза

### 4. Определение позиций

Стол 6-max:
```
        [3]
    [2]     [4]
[1]             [5]
       [0]
     (Hero)
```

Относительная позиция от дилера:
- 0 = dealer (button)
- 1 = small blind
- 2 = big blind
- 3 = UTG (early position)
- 4 = MP (middle position)
- 5 = CO (cutoff)

---

## Режимы работы

### Параметры в начале файла

```python
# БЕЗОПАСНОСТЬ
DRY_RUN = True           # True = не кликает, False = реальная игра

# ОТЛАДКА
DEBUG_MODE = True        # Выводит подробную информацию
SAVE_DEBUG_IMAGES = True # Сохраняет скриншоты для проверки

# API
API_BASE_URL = "http://localhost:8000/api/v1"
API_KEY = "dev_admin_key"

# ИГРА
LIMIT_TYPE = "NL10"      # или "NL50"
SESSION_ID = "pppoker_adb_session"
POLL_INTERVAL = 1.0      # Как часто проверять (секунды)
```

### Настройка для разных экранов

Если у вас другое разрешение экрана или PPPoker выглядит иначе, отредактируйте:

```python
# Позиции игроков (проценты от размера экрана)
PLAYER_SCREEN_POSITIONS = {
    0: (0.50, 0.75),  # Hero - внизу по центру
    1: (0.15, 0.65),  # Лево-низ
    2: (0.10, 0.35),  # Лево-верх
    3: (0.50, 0.20),  # Верх центр
    4: (0.90, 0.35),  # Право-верх
    5: (0.85, 0.65),  # Право-низ
}
```

Также можете настроить регионы карт, пота, кнопок в классе `EnhancedPokerParser.__init__()`.

---

## Troubleshooting

### Проблема: "Не найдено подключенных Android устройств"

**Решение:**
```bash
# Проверьте подключение
adb devices

# Если пусто - переподключите USB
# Если "unauthorized" - разрешите на телефоне

# Перезапустите ADB сервер
adb kill-server
adb start-server
adb devices
```

### Проблема: "Не удалось распознать карты героя"

**Причины:**
1. Неправильные координаты для вашего экрана
2. PPPoker в другой теме (тёмная/светлая)
3. Разрешение экрана отличается

**Решение:**
1. Включите `SAVE_DEBUG_IMAGES = True`
2. Запустите бота
3. Откройте файлы `debug_*.png`
4. Проверьте что в файле `debug_hero_cards.png` видны карты
5. Если нет - настройте координаты `hero_cards_region`

### Проблема: "Backend API недоступен"

**Проверьте:**
```bash
# Backend запущен?
curl http://localhost:8000/health

# Правильный API ключ?
curl -H "X-API-Key: dev_admin_key" http://localhost:8000/api/v1/decision
```

### Проблема: "Бот не кликает по кнопкам"

**Причины:**
1. `DRY_RUN = True` (это нормально!)
2. Неправильные координаты кнопок

**Решение:**
1. Убедитесь что `DRY_RUN = False`
2. Проверьте логи: должны быть `✅ Клик: (x, y)` а не `🔷 [DRY RUN]`

### Проблема: "Низкая точность OCR"

**Улучшение точности:**
1. Увеличьте разрешение экрана Android
2. Используйте светлую тему PPPoker
3. Настройте параметры Tesseract:
   ```python
   config = '--psm 7 --oem 3 -c tessedit_char_whitelist=...'
   ```

### Проблема: "Не находит баттон дилера"

**Причины:**
- Баттон другого цвета/формы в вашей версии PPPoker

**Решение:**
Откройте `debug_players.png` и проверьте - помечен ли дилер зелёным кругом?
Если нет - настройте параметры цвета в `_check_dealer_button_near()`:
```python
lower_white = np.array([0, 0, 200])
upper_white = np.array([180, 30, 255])
```

---

## 📊 Мониторинг

### Консольный вывод

Бот показывает:
```
🎴 Карты героя: AsKh
🎴 Борд: 9d7c2s
💰 Пот: $3.50
🎯 Улица: flop
👥 Активных игроков: 4
🤖 Решение AI: RAISE $2.50
✅ Клик: (650, 920)
📊 Сыграно рук: 42
```

### Web Dashboard

Откройте http://localhost:3000 для просмотра:
- Статистика в реальном времени
- История раздач
- Винрейт
- Графики

---

## 🎯 Статус проекта: 100% ЗАВЕРШЁН

### Реализовано:

✅ **Backend API** (149 тестов проходят)
- FastAPI + SQLAlchemy
- PostgreSQL в Docker
- Prometheus метрики
- API ключи

✅ **Frontend Dashboard**
- React + TypeScript
- Real-time WebSocket обновления
- Admin панель
- Графики и статистика

✅ **PPPoker ADB Bot** (этот файл)
- Полное распознавание экрана
- Computer Vision для баттона
- OCR для карт/стеков/ставок
- Автоматические действия
- Интеграция с Backend

✅ **Безопасность**
- DRY_RUN режим
- Debug режим
- Логирование всех действий

---

## 🚀 Следующие шаги

1. **Протестируйте в DRY_RUN режиме:**
   ```bash
   python3 pppoker_adb_bot_full.py
   ```

2. **Проверьте debug изображения:**
   ```bash
   open debug_*.png
   ```

3. **Убедитесь что всё распознаётся правильно**

4. **Настройте координаты если нужно**

5. **Включите реальную игру:**
   - Измените `DRY_RUN = False`
   - Запустите снова

6. **Мониторьте через Dashboard:**
   - http://localhost:3000

---

## ⚠️ Важные замечания

1. **Юридические аспекты:**
   - Использование ботов может нарушать правила покер-румов
   - Используйте на свой риск
   - Только для образовательных целей

2. **Технические ограничения:**
   - Точность OCR зависит от качества экрана
   - Координаты могут отличаться на разных устройствах
   - Требуется калибровка под ваш экран

3. **Производительность:**
   - Бот опрашивает экран каждые `POLL_INTERVAL` секунд
   - Слишком частое опрашивание = больше нагрузка
   - Слишком редкое = можем пропустить ход

---

## 📝 Логи и отладка

### Включение подробных логов

В начале файла добавьте:
```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

### Структура debug файлов

При `SAVE_DEBUG_IMAGES = True`:
- `debug_hero_cards.png` - регион ваших карт
- `debug_board.png` - регион борда
- `debug_pot.png` - регион пота
- `debug_stack_0.png` ... `debug_stack_5.png` - стеки игроков
- `debug_bet_0.png` ... `debug_bet_5.png` - ставки игроков
- `debug_players.png` - весь стол с отметками игроков

**Дилер помечен зелёным кругом, остальные - жёлтым**

---

## 🎓 Дополнительная информация

### Файлы проекта

- `pppoker_adb_bot_full.py` - основной файл бота
- `PPPOKER_BOT_README.md` - это руководство
- `table_agent/screen_reader.py` - базовые классы для screen reading
- `api/` - Backend API
- `frontend/` - Web Dashboard

### Связанные проекты

Backend API работает на порту 8000:
```bash
# Запуск через Docker
docker-compose up -d

# Или локально
python3 -m uvicorn api.main:app --reload
```

Frontend Dashboard на порту 3000:
```bash
cd frontend
npm install
npm start
```

---

## 💡 Советы по оптимизации

1. **Настройте POLL_INTERVAL:**
   - 0.5 сек = быстрая реакция, больше нагрузка
   - 2.0 сек = медленная, меньше нагрузка

2. **Калибровка координат:**
   - Сыграйте несколько рук в DRY_RUN
   - Проверьте все debug_*.png файлы
   - Настройте координаты для 100% точности

3. **Улучшение OCR:**
   - Увеличьте яркость экрана Android
   - Используйте светлую тему
   - Отключите анимации в PPPoker

4. **Производительность:**
   - Закройте ненужные приложения на Android
   - Отключите автоповорот экрана
   - Включите "Оставаться в режиме ожидания"

---

## ✉️ Поддержка

Если возникли проблемы:
1. Проверьте этот README
2. Включите DEBUG_MODE и проверьте логи
3. Проверьте debug_*.png файлы
4. Убедитесь что Backend API работает

---

**Удачной игры! 🎰♠️♥️♦️♣️**
